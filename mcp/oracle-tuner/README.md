# MCP Oracle Tuner

Oracle SQL 메타데이터를 기반으로 규칙 기반 튜닝 권고를 생성하는 MCP 서버입니다.

## 실행

```
npm run dev --workspace=@qwen-code/mcp-oracle-tuner
```

## MCP 도구

- `oracle.tune_from_metadata`

## 입력 예시

`oracle.tune_from_metadata` 도구는 `oracle-meta-gateway`의 결과를 그대로 전달받는 형태를 권장합니다.
